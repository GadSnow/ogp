import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
    selector: 'app-remise',
    imports: [RouterOutlet],
    templateUrl: './remise.html',
    styleUrl: './remise.scss'
})
export class Remise {}
